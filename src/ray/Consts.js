// Actual Constant that we will never change
const TWO_PI = 6.28318530718;

const MAX_DISTANCE = 2;
const EPSILON = 1e-6;

const N = 32;
const MAX_STEP = 1;

const WIDTH = 256;
const HEIGHT = 256;

export {TWO_PI, N, MAX_STEP, MAX_DISTANCE, EPSILON, WIDTH, HEIGHT};
