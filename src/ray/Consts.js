// Actual Constant that we will never change
const TWO_PI = 6.28318530718;
const BIAS = 1e-4;
const MAX_DISTANCE = 2;
const EPSILON = 1e-6;

const N = 8;
const MAX_STEP = 8;
// 递归求反射/折射深度
const MAX_DEPTH = 2;

const WIDTH = 256;
const HEIGHT = 256;

export {TWO_PI, N, MAX_STEP, MAX_DISTANCE, MAX_DEPTH, EPSILON, BIAS, WIDTH, HEIGHT};
